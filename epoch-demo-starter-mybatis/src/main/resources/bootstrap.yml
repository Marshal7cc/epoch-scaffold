spring:
  application:
    name: epoch-demo-starter-mybatis
  profiles:
    active: local

  # nacos local mode
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false
