spring:
  application:
    name: epoch-scaffold-jpa
  profiles:
    active: local

  # nacos local mode
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false
