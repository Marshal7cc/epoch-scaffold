spring:
  application:
    name: epoch-scaffold-mybatis
  profiles:
    active: local

  # nacos local mode
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false
