spring:
  application:
    name: epoch-demo-starter-jpa
  profiles:
    active: local

  # nacos local mode
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false
